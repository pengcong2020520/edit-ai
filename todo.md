# editAI 第二阶段 Todo

维护规则：继续开发前先读取本文件；完成或调整任务后同步更新状态。

## 已完成

- [x] 确认第二阶段需求：项目化写作工作台、右侧采访/分析双模式、主区可编辑草稿。
- [x] 新增后端 `editai_note` 目录边界：文件树、引用、读取、保存都限制在默认工作目录的 `editai_note` 下。
- [x] 新增项目 API：读取项目列表、创建项目、默认生成 `draft.md`。
- [x] 新增后端任务模式：`super-interviewer` 与 `super-analyst`。
- [x] 重构前端入口：首次进入先创建或选择文章项目，再进入写作界面。
- [x] 重构主写作区：使用当前 contenteditable Markdown 渲染编辑方案承载 `draft.md`。
- [x] 重构右侧栏：只保留采访/分析切换、启动按钮和历史折叠栏。
- [x] 顶部去除 agent 状态，改为模式名前的启动状态点。
- [x] 补齐前端样式，确保项目入口、写作区、右侧历史栏在一屏内稳定展示。
- [x] 修正 `.env` 读取时机：`PORT`、`EDITAI_WEB_ROOT` 等轻量配置可从 `.env` 生效。
- [x] 运行 `bun run build` 通过，并还原构建脚本带出的无关 schema 漂移。
- [x] 处理 `super-interviewer` 自动最多 3 轮后的继续启动体验。
- [x] 确认 `approve` 后用户修改内容能稳定进入下一轮上下文。
- [x] 用 Playwright 验证：创建项目、读取 `draft.md`、保存、采访/分析流程、右侧折叠历史、@引用。
- [x] 验证切换默认工作目录后会自动创建/读取新的 `editai_note`。
- [x] 修复主编辑区内容在采访/分析后被覆盖导致用户输入丢失的问题。
- [x] 验证用户输入要求会和当前草稿一起传入后端，返回后主区呈现修改后的完整文章。
- [x] 增加空项目写作入口：先对话澄清，再由主编生成初稿，用户确认后进入采访/分析。
- [x] 验证空项目初稿流程：主编模式生成 `## 文章草稿`、敲定前隐藏 Approve、敲定后启用采访/分析。
- [x] 修复第三阶段流程执行：大纲/初稿/精修/核查/评分按阶段主按钮真正执行。
- [x] 修复正文更新策略：右侧普通建议不覆盖正文，明确润色/阶段任务/勾选应用才更新正文。
- [x] 修复顶部流程不可逆：已完成和未来阶段不可点击回退，只能通过主按钮推进。
- [x] Playwright 验证：流程不可逆、阶段 mode 正确、普通右侧建议不覆盖正文、勾选应用后推进。
- [x] 修复初稿生成：即使模型未包裹 `## 文章草稿`，也能提取完整初稿落到正文。
- [x] 修复精修/事实核查/质量评分：报告显示在右侧栏，勾选选项也在右侧栏。
- [x] 修复勾选应用：按用户选中的意见调用 Editor 修改正文，并保持正文只包含文章本体。
- [x] Playwright 验证：初稿、精修报告、核查报告、质量评分报告、勾选应用和正文纯净格式。
- [x] 修复初稿提取：兼容 `### 文章草稿`、Markdown 代码块、以及“以下是完整初稿”这类真实模型输出格式。
- [x] Playwright 验证：格式不标准时，初稿仍能写入文本编辑区，并去除代码块/说明性前缀。
- [x] 修复响应分节边界：文章里的 `# / ##` 标题不再截断 `文章草稿` 内容。
- [x] 修复终稿保存空文件风险：编辑区读取为空时使用最近一次正文 Markdown 兜底，并阻止空终稿保存。
- [x] Playwright 验证：大纲有内容、初稿完整、事实核查不覆盖正文、final.md 非空。
- [x] 移除内容精修/事实核查的勾选建议 UI，改为右侧只展示报告，通过聊天继续要求修改正文。
- [x] 修正初稿确认流程：确认初稿只推进到内容精修，精修报告由内容精修阶段单独生成。
- [x] Playwright 验证：精修/核查报告不覆盖正文，右侧聊天仍可调用编辑并更新正文。
- [x] 针对模型服务繁忙增加 OpenAI-compatible 环境变量入口和自动退避重试，保留 DeepSeek 原配置兼容。
- [x] 实测 DeepSeek 当前不可用：直接 API 请求出现 503 或超时；本地 `/api/tasks` 已能在超时后返回中文错误，不再无限 running。
- [x] 已将第三阶段写作工作流、模型容错和前端交互优化提交并推送到 GitHub。
- [x] 新增简易本地部署说明和本地部署打包脚本，生成可分发压缩包并排除私密/本地数据。

## 进行中

- [ ] 等待用户更换可用 OpenAI-compatible 供应商或 DeepSeek 服务恢复后继续验收真实生成效果。

## 待完成

- [x] 第三阶段流程重构：中间纯编辑决策、右侧统一对话、顶部流程进度。
- [x] 实现流程：选题交互 -> 大纲框架 -> 初稿敲定 -> 内容精修 -> 事实核查 -> 质量评分 -> 终稿敲定。
- [x] 右侧对话按当前阶段自动调度 agent/命令，并保持多轮上下文。
- [x] 用户进入系统时支持导入历史文章，提取全局风格指纹；用户也可跳过。
- [x] 内容精修和事实核查支持勾选建议，提交后覆盖中间文章。
- [x] `draft.md` 持续保存，终稿保存为 `final.md`。
- [x] Playwright 验证流程：跳过风格、创建项目、右侧对话、流程勾选、终稿保存。
